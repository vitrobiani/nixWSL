local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "cpp", "c", "cmake"},

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
